# Copia este archivo como .env para desarrollo local con docker-compose
# NUNCA subas el archivo .env real al repositorio (ya está en .gitignore)

DB_ROOT_PASSWORD=rootpass123
DB_NAME=examendb
DB_USER=appuser
DB_PASSWORD=apppass123
